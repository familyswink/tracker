# Archived patch scripts

These one-off `apply_*.py` / `_patch_ui.py` tools string-replaced `index.html` during early development. They are **retired** as of Phase 1 (2026-05-19).

Use `src/` modules, `npm test`, and `npm run build` (`scripts/inline-bundle.mjs`) instead.
